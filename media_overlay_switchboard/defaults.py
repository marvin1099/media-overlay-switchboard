# Default constants for media-overlay-switchboard

PROJECT_NAME = "media-overlay-switchboard"

# Default socket suffix when none is provided
DEFAULT_SUFFIX = "default"

# Separator between text entries when reading the text file
DEFAULT_TEXT_SEPARATOR = "-- TEXTSPLIT --"

# Default size for the transparent placeholder image (width, height)
DEFAULT_TRANSPARENT_SIZE = (1920, 1080)

# Filenames written into the target output folder
OUTPUT_TEXT_FILE = "overlay_text.txt"
OUTPUT_TEXT_HTML_FILE = "overlay_text.html"
OUTPUT_IMAGE_FILE = "overlay_image.png"

# Recognised image file extensions (lowercase for matching)
IMAGE_EXTENSIONS = {".png", ".jpg", ".jpeg", ".gif", ".bmp", ".webp"}
